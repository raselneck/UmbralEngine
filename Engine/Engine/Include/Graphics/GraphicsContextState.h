#pragma once

/**
 * @brief An enumeration of possible states when setting the active graphics context.
 */
enum class EGraphicsContextState : bool
{
	Unavailable = false,
	Available = true
};